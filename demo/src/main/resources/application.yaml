server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/mydb"
    username: root
    password: truongdz002
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
